{
  "scripts": {
    "test": "node repo-guards.test.mjs && node workflow-engine.test.mjs && node workflow-effects.test.mjs && node workflow-templates.test.mjs && node workflow-versions.test.mjs && node workflow-builder-handshake.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-ez \"node firestore-rules.test.mjs\"",
    "test:all": "npm test && npm run test:rules"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.0"
  }
}
